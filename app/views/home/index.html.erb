<% if user_devise_signed_in? %>
  <!-- Blank for now -->
  <h1> You are logged in!</h1>
  <% link_to "Log out", destroy_user_devise_session_path, data: { "turbo-method": :delete, class: "btn btn-primary btn-lg"} %>
<% else%>
  <h1>hello world</h1>
  <%= link_to "Sign up", new_user_devise_registration_path, class: "btn btn-primary btn-lg" %>
   <%= link_to "Log in", new_user_devise_session_path, class: "btn btn-outline-primary btn-lg" %>
<% end %>